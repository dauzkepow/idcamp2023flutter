//Rangkuman Materi
/*
  1. if = Untuk mengakomodasi dan mengecek sebuah kondisi
     jika hasil ekspresi bernilai benar, blok kode didalamnya dijalankan
     jika false menggunakan ekspresi else
  2. contoh if-else
     String calculateScore(score) {
      if (score > 90) {
        return 'A'; //return sesuai tipe data nama String
      } else if (score > 80) {
        return 'B';
      } else if (score > 70) {
        return 'C';
      } else if (score > 60) {
        return 'D';
      } else {
        return 'E';
      }
    }

  3. menuliskan perintah kode secara berulang menggunakan for, while, do-while
  FOR :
      for (int i = 1; i <= 100; i++) {
      print(i);
    }
  WHILE (cek dulu sebelum perulangan):
      var i = 1;
 
      while (i <= 100) {
        print(i);
        i++;
      }
  DO-WHILE (perulangan satu kali baru dicek kondisi):
      var i = 1;

      do {
        print(i); //menjalankan blok kode satu kali
        i++;
        } while (i <= 15); //variabel dievaluasi setelah blok kode {} dijalankan
      }
  
  4. Break = menghentikan / keluar dari proses iterasi
  5. Continue = melewatkan proses iterasi dan melanjutkan ke iterasi selanjutnya
  6. Switch 
      switch (variable/expression) {
      case value1:
        // do something
        break;
      case value2:
        // do something
        break;
      …
      ...
      default:
        // do something else
    }


  video pembahasan dari dicoding :
  https://www.youtube.com/watch?v=-yp78PXFj9U&t=10s

*/
